\documentclass{minimal}
\usepackage[a4paper,margin=1cm,landscape]{geometry}
\usepackage{tikz}
\usetikzlibrary{positioning,shapes,shadows,arrows}

\begin{document}
\tikzstyle{hierarchy1}=[rectangle, draw=black, rounded corners, fill=gray!40, drop shadow,
        text centered, anchor=north, text=black, text width=4cm]
\tikzstyle{hierarchy2}=[rectangle, draw=black, rounded corners, fill=white, drop shadow,
        text centered, anchor=north, text=black, text width=4cm]
\tikzstyle{hierarchy3}=[rectangle, draw=black, rounded corners, fill=white, drop shadow,
        text centered, anchor=north, text=black, text width=4cm]
\tikzstyle{myarrow}=[->, >=open triangle 90, thick]
\tikzstyle{line}=[-, thick]
        
\begin{center}
\begin{tikzpicture}[node distance=2cm]
  \node (Arena) [hierarchy1, rectangle split, rectangle split parts=2]
  {
    \textbf{ARENA}
    \nodepart{second} n = numeric \\ m = numeric \\ iter = numeric \\ smax = numeric \\ seed = numeric \\ epsilon = numeric
  };
  
  \node (Substance) [hierarchy2, rectangle split, rectangle split parts=2, below left=of Arena]
  {
    \textbf{SUBSTANCES}
    \nodepart{second} diffconst = numeric \\ diffmatrix = numeric \\ diffusion() \\ getConc()
  };

  \node (Organism) [hierarchy2, rectangle split, rectangle split parts=2, below right= of Arena]
  {
    \textbf{ORGANISM}
    \nodepart{second} x = numeric \\ y = numeric \\ objective = character \\ model = modelorg \\ optimize() \\ evaluate()
  };

  \node (Bac) [hierarchy3, rectangle split, rectangle split parts=2, below= of Organism]
  {
    \textbf{BAC}
    \nodepart{second} bug 
  };


  \draw[myarrow] (Substance.north) -- ++(0,0.8) -| (Arena.south);
  \draw[line] (Substance.north) -- ++(0,0.8) -| (Organism.north);

  \draw[myarrow] (Bac.north) -- ++(0,0.8) -| (Organism.south);
\end{tikzpicture}
\end{center}
\end{document}
