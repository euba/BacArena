\documentclass{minimal}
\usepackage[a4paper,margin=1cm,landscape]{geometry}
\usepackage{tikz}
\usetikzlibrary{positioning,shapes,shadows,arrows}

\begin{document}
\tikzstyle{hierarchy1}=[rectangle, draw=black, rounded corners, fill=gray!40, drop shadow,
        text centered, anchor=north, text=black, text width=4cm]
\tikzstyle{hierarchy2}=[rectangle, draw=black, rounded corners, fill=white, drop shadow,
        anchor=north, text=black, text width=4cm]
\tikzstyle{hierarchy3}=[rectangle, draw=black, rounded corners, fill=white, drop shadow,
        text centered, anchor=north, text=black, text width=4cm]
\tikzstyle{myarrow}=[->, >=open triangle 90, thick]
\tikzstyle{line}=[-, thick]
        
\begin{center}
\begin{tikzpicture}[node distance=2cm]
  \node (Arena) [hierarchy2, rectangle split, rectangle split parts=3]
  {
  	\begin{center}
    \textbf{Arena}
    \end{center}
    \nodepart{second} orgdat = data.frame \\ specs = list \\ media = list \\ phenotypes = list \\ mediac = character \\ occmat = Matrix \\ tstep = numeric \\ stir = logical \\ n = integer \\ m = integer
    \nodepart{third} addOrg() \\ addSubs() \\ changeSub() \\ changeOrg() \\ checkPhen() \\ simulate() \\ stirEnv() \\ dat2mat() \\ changeDiff() \\ getSublb()
  };
  
  \node (Eval) [hierarchy2, rectangle split, rectangle split parts=3, below = of Arena]
  {
    \begin{center}
    \textbf{Eval}
    \end{center}
    \nodepart{second} medlist = list \\ simlist = list \\ subchange = numeric
    \nodepart{third} addEval() \\ evalArena() \\ getArena() \\ extractMed() \\ plotCurves() \\ getPhenoMat() \\ minePheno()
  };  
  
  \node (Substance) [hierarchy2, rectangle split, rectangle split parts=3, right=of Arena]
  {
    \begin{center}
    \textbf{Substance}
    \end{center}
    \nodepart{second} smax = numeric \\ diffmat = Matrix \\ name = character \\ difunc = character \\ difspeed = numeric
    \nodepart{third} diffuseR() \\ diffuseCpp()
  };

  \node (Organism) [hierarchy2, rectangle split, rectangle split parts=3, right= of Substance]
  {
    \begin{center}
    \textbf{Organism}
    \end{center}
    \nodepart{second} lbnd = numeric \\ ubnd = numeric \\ type = character \\ medium = character \\ lpobj = sysBiolAlg \\ fbasol = list \\ lyse = logical \\ feat = list \\ deathrate = numeric \\ duplirate = numeric \\ growthlimit = numeric \\ growtype = character
    \nodepart{third} constrain() \\ consume() \\ emptyHood() \\ getHood() \\ getPhenotype() \\ growExp() \\ growLin() \\ optimizeLP() \\ lysis()
  };

  \node (Bac) [hierarchy2, rectangle split, rectangle split parts=3, below left = of Organism]
  {
    \begin{center}
    \textbf{Bac}
    \end{center}
    \nodepart{second} speed = integer \\ budge = logical \\ chem = character
    \nodepart{third} growth() \\ move() \\ chemotaxis() \\ budging() \\ simBac()
  };
  
  \node (Human) [hierarchy2, rectangle split, rectangle split parts=3, below right = of Organism]
  {
    \begin{center}
    \textbf{Human}
    \end{center}
    \nodepart{second} objective = character
    \nodepart{third} changeFobj() \\ cellgrowth() \\ simHum()
  };


  \draw[myarrow] (Eval.north) -- ++(0,0.8) -| (Arena.south);
  %\draw[line] (Eval.north) -- ++(0,0.8) -| (Arena.south);

  \draw[myarrow] (Bac.north) -- ++(0,0.8) -| (Organism.south);
  \draw[line] (Human.north) -- ++(0,0.8) -| (Bac.north);
\end{tikzpicture}
\end{center}
\end{document}
